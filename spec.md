# Tsurara 要件仕様書（MVP）

## 概要

**Tsurara** は、macOS のメニューバーアイコンを整理するアプリである。
ノッチ搭載 Mac ではメニューバーの表示領域が狭く、常駐アプリのアイコンが増えるとノッチの裏に隠れて操作できなくなる。
Tsurara は、普段使わないアイコンを非表示にし、必要なときだけワンクリックで呼び出せるようにする。

参考にする既存アプリは [Ice](https://github.com/jordanbaird/Ice) である。
本仕様は Ice の機能のうち、アイコンの表示と非表示の管理だけを MVP として切り出したものである。
外観カスタマイズや Ice Bar 相当の機能は後続フェーズとする（[後続フェーズ](#後続フェーズ) を参照）。

## 用語

- **セクション**：メニューバー上のアイコンの区画。Tsurara は次の 3 つを管理する。
  - **表示セクション**：常に表示するアイコンの区画。メニューバー右端（時計側）に位置する。
  - **非表示セクション**：普段は隠し、トグル操作で一時的に表示するアイコンの区画。
  - **常時非表示セクション**：トグル操作でも表示しないアイコンの区画。設定画面からのみ表示を解除できる。
- **区切りアイテム**：セクションの境界を示すために Tsurara 自身がメニューバーに置く NSStatusItem。
- **トグル操作**：非表示セクションの表示と非表示を切り替える操作。

## スコープ

MVP に含める機能は次のとおりである。

1. 非表示セクションのトグル（クリックとホットキー）
2. 常時非表示セクション
3. 自動再非表示
4. 設定画面（ホットキー割り当て、ログイン時起動、自動再非表示の秒数）
5. ログイン時起動

MVP に含めない機能は次のとおりである。

- メニューバーの外観カスタマイズ（ティント、シャドウ、ボーダー、形状）
- Ice Bar 相当の別バー表示
- アイコンの検索
- レイアウトプロファイル
- ドラッグ＆ドロップによる設定画面上でのアイコン並べ替え
- 自動更新

## 機能要件

### セクションの実現方式

Tsurara はメニューバーに 2 つの区切りアイテムと 1 つのトグルアイテムを置く。

```
[常時非表示セクション] ◆ [非表示セクション] ◇ [表示セクション ❄]（時計側）
```

- `◇`（メイン区切り）より右が表示セクション、左が非表示セクションである。
- `◆`（サブ区切り）より左が常時非表示セクションである。
- `❄`（トグルアイテム）は表示セクション内に常に見えており、クリックでトグルを実行する。
  区切りアイテム自身の length を拡大するとそのアイコンは画面外へ流れてクリック
  できなくなるため、クリック対象を区切りから分離している（Hidden Bar / Ice と同方式）。
- 利用者は Cmd キーを押しながらアイコンをドラッグし、区切りアイテムを基準に各アプリのアイコンを配置する。

非表示は、区切りアイテムの `length` を拡大して左側のアイコンを画面外へ押し出す方式で実現する。
この方式を選ぶ理由は、パブリック API だけで実装でき、アクセシビリティ権限や画面収録権限を要求せずに済むからである[^approach]。

[^approach]: 代替案として、ScreenCaptureKit でメニューバーを描画し直す方式（Ice の外観カスタマイズが採る方式）があるが、画面収録権限が必要になるうえ MVP の範囲では利点がない。

### トグル操作

非表示セクションは次の操作で表示と非表示を切り替えられる。

- トグルアイテムのクリック
- グローバルホットキー（既定は未割り当て。設定画面で割り当てる）

トグルアイテムは、状態がわかるようにシンボルを変える。
非表示中は「つらら」を模したアイコン、表示中は開いた状態のアイコンとする。

### 自動再非表示

非表示セクションを表示した後、一定時間が経過したら自動で再非表示にする。
既定は有効、時間は 15 秒とする。
設定画面で無効化と秒数変更（5 秒から 60 秒）ができる。
利用者がメニューバーのアイコンを操作している間（いずれかのメニューが開いている間）は、タイマーを進めない。

### 常時非表示セクション

サブ区切りより左のアイコンは、トグル操作でも表示しない。
設定画面の「常時非表示セクションを一時的に表示する」ボタンでのみ表示できる。
このセクションを使わない利用者のために、設定画面でサブ区切り自体を無効化できる（既定は無効）。

### 設定画面

SwiftUI の Settings ウィンドウとして実装する。設定項目は次のとおりである。

- **一般**：ログイン時起動、常時非表示セクションの有効化、自動再非表示の有効化と秒数
- **ホットキー**：非表示セクションのトグル

設定は `UserDefaults` に保存する。

### ログイン時起動

`SMAppService` を使い、設定画面のチェックボックスで登録と解除を行う。

## 非機能要件

- **対象 OS**：macOS 14（Sonoma）以降
- **言語とフレームワーク**：Swift、SwiftUI（メニューバー操作は AppKit の NSStatusItem を併用）
- **配布**：当面は手動ビルド。コード署名と公証、Homebrew 対応は後続フェーズで検討する
- **Dock 非表示**：`LSUIElement` を有効にし、Dock アイコンを持たない常駐アプリとする
- **権限**：特別な権限（アクセシビリティ、画面収録）を要求しない

## アーキテクチャ概要

責務を次の単位に分ける。

- **MenuBarSection**：1 セクションの状態（表示中か、区切りアイテムの参照）を持つモデル
- **SectionManager**：全セクションの生成と、トグルおよび自動再非表示の制御
- **HotkeyManager**：グローバルホットキーの登録と、押下イベントの SectionManager への通知
- **SettingsStore**：設定値の読み書き（`UserDefaults` の薄いラッパー）
- **SettingsView**：設定画面（SwiftUI）

UI 層（SettingsView、区切りアイテムの見た目）とロジック層（SectionManager、SettingsStore）を分離し、ロジック層は AppKit への依存をプロトコルで抽象化してテスト可能にする。

## テスト方針

t_wada 流の TDD で進める。実装より先にテストを書き、レッド、グリーン、リファクタリングの順で回す。

- **ユニットテスト**：SectionManager の状態遷移（トグル、自動再非表示のタイマー制御）、SettingsStore の読み書きを対象とする。NSStatusItem はプロトコルで差し替え、モックで検証する
- **手動確認項目**：実機での区切りアイテムの押し出し動作、Cmd ドラッグでの配置、ログイン時起動。これらは OS 統合部分でありユニットテストの対象外とする

## 後続フェーズ

MVP の完成後、次の順で拡張を検討する。

1. Ice Bar 相当（あふれたアイコンをノッチ下の別バーに表示）
2. メニューバーの外観カスタマイズ
3. アイコンの検索とレイアウトプロファイル
4. コード署名、公証、Homebrew 配布
