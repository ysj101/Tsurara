# Tsurara 要件仕様書（MVP）

## 概要

**Tsurara** は、macOS のメニューバーアイコンを整理するアプリである。
ノッチ搭載 Mac ではメニューバーの表示領域が狭く、常駐アプリのアイコンが増えるとノッチの裏に隠れて操作できなくなる。
Tsurara は、普段使わないアイコンを非表示にし、必要なときだけワンクリックで呼び出せるようにする。

参考にする既存アプリは [Ice](https://github.com/jordanbaird/Ice) である。
本仕様は Ice の機能のうち、アイコンの非表示管理と **Ice Bar 相当のサブバー表示**を MVP とする。
ノッチ搭載 Mac ではメニューバー内へアイコンを「再表示」しても表示幅が足りず見えないため、
非表示にしたアイコンは Tsurara アイコンの直下に別のバー（サブバー）として並べて表示し、
そこから操作できるようにする。外観カスタマイズは後続フェーズとする（[後続フェーズ](#後続フェーズ) を参照）。

## 用語

- **セクション**：メニューバー上のアイコンの区画。Tsurara は次の 3 つを管理する。
  - **表示セクション**：常に表示するアイコンの区画。メニューバー右端（時計側）に位置する。
  - **非表示セクション**：普段はメニューバーから隠し、サブバーに表示するアイコンの区画。
  - **常時非表示セクション**：サブバーにも表示しないアイコンの区画。設定画面からのみ表示を解除できる。
- **区切りアイテム**：セクションの境界を示すために Tsurara 自身がメニューバーに置く NSStatusItem。
- **サブバー**：Tsurara アイコンのクリックで、その直下に表示されるバー。非表示セクションの
  アイコンを並べて表示し、クリックで元のアイコンと同じ操作（メニュー表示など）ができる。
- **トグル操作**：サブバーの表示と非表示を切り替える操作。

## スコープ

MVP に含める機能は次のとおりである。

1. 非表示セクション（length 押し出しによる恒常的な非表示）
2. サブバー（Tsurara アイコンのクリックとホットキーで開閉。非表示アイコンの表示と操作）
3. 常時非表示セクション
4. サブバーの自動クローズ
5. 設定画面（ホットキー割り当て、ログイン時起動、自動クローズの秒数）
6. ログイン時起動

MVP に含めない機能は次のとおりである。

- メニューバーの外観カスタマイズ（ティント、シャドウ、ボーダー、形状）
- アイコンの検索
- レイアウトプロファイル
- ドラッグ＆ドロップによる設定画面上でのアイコン並べ替え
- 自動更新

## 機能要件

### セクションの実現方式

Tsurara はメニューバーに 2 つの区切りアイテムと 1 つのトグルアイテムを置く。

```
[常時非表示セクション] ◆ [非表示セクション] ◇ [表示セクション ❄]（時計側）
```

- `◇`（メイン区切り）より右が表示セクション、左が非表示セクションである。
- `◆`（サブ区切り）より左が常時非表示セクションである。
- `❄`（Tsurara アイコン）は表示セクション内に常に見えており、クリックでサブバーを開閉する。
- 利用者は Cmd キーを押しながらアイコンをドラッグし、区切りアイテムを基準に各アプリのアイコンを配置する。

非表示は、区切りアイテムの `length` を拡大して左側のアイコンを画面外へ押し出す方式で実現し、
**メニューバー内への再展開は行わない**（ノッチ搭載 Mac では再展開しても表示幅が足りないため）。
非表示アイコンへのアクセスはサブバーが担う。

サブバーの実現には、非表示アイコンの見た目の取得（ScreenCaptureKit によるメニューバーの撮像）が
必要であり、**画面収録権限を要求する**。クリック操作の転送方式（実アイテムの一時表示 +
合成クリック等）は実装時に調査・確定する（アクセシビリティ権限が追加で必要になる可能性がある）[^approach]。

[^approach]: 旧仕様では権限不要を優先し「length 復元でメニューバー内に再表示するトグル」を
採用していたが、ノッチ搭載 Mac では再表示分の幅が確保できず実用にならないため、
権限要求と引き換えにサブバー方式（Ice の Ice Bar 相当）へ変更した。

### サブバー

サブバーは次の操作で開閉できる。

- Tsurara アイコンのクリック
- グローバルホットキー（既定は未割り当て。設定画面で割り当てる）

サブバーは Tsurara アイコンの直下に表示され、非表示セクションのアイコンを
メニューバーと同じ並び順で表示する。各アイコンはクリックで元のアイコンと同じ操作
（メニュー表示など）ができる。サブバーの外側をクリックすると閉じる。

Tsurara アイコンは、状態がわかるようにシンボルを変える。
サブバーが閉じているときは「つらら」を模したアイコン、開いているときは開いた状態のアイコンとする。

### サブバーの自動クローズ

サブバーを開いた後、一定時間が経過したら自動で閉じる。
既定は有効、時間は 15 秒とする。
設定画面で無効化と秒数変更（5 秒から 60 秒）ができる。
利用者がサブバー内のアイコンを操作している間（いずれかのメニューが開いている間）は、タイマーを進めない。

### 常時非表示セクション

サブ区切りより左のアイコンは、サブバーにも表示しない。
設定画面の「常時非表示セクションを一時的に表示する」ボタンでのみ表示できる。
このセクションを使わない利用者のために、設定画面でサブ区切り自体を無効化できる（既定は無効）。

### 設定画面

SwiftUI の Settings ウィンドウとして実装する。設定項目は次のとおりである。

- **一般**：ログイン時起動、常時非表示セクションの有効化、サブバー自動クローズの有効化と秒数
- **ホットキー**：サブバーの開閉

設定は `UserDefaults` に保存する。

### ログイン時起動

`SMAppService` を使い、設定画面のチェックボックスで登録と解除を行う。

## 非機能要件

- **対象 OS**：macOS 14（Sonoma）以降
- **言語とフレームワーク**：Swift、SwiftUI（メニューバー操作は AppKit の NSStatusItem を併用）
- **配布**：当面は手動ビルド。コード署名と公証、Homebrew 対応は後続フェーズで検討する
- **Dock 非表示**：`LSUIElement` を有効にし、Dock アイコンを持たない常駐アプリとする
- **権限**：サブバーの撮像のために画面収録権限を要求する。初回はオンボーディングで
  目的を説明し、拒否された場合はサブバーなし（非表示のみ）で動作を継続する

## アーキテクチャ概要

責務を次の単位に分ける。

- **MenuBarSection**：1 セクションの状態（表示中か、区切りアイテムの参照）を持つモデル
- **SectionManager**：全セクションの生成と、サブバー開閉および自動クローズの制御
- **MenuBarItemImaging**：ScreenCaptureKit による非表示アイコンの検出・撮像（画面収録権限）
- **SubBarPanel**：サブバーの表示（NSPanel）。撮像したアイコンを並べ、クリックを実アイテムへ転送する
- **HotkeyManager**：グローバルホットキーの登録と、押下イベントの SectionManager への通知
- **SettingsStore**：設定値の読み書き（`UserDefaults` の薄いラッパー）
- **SettingsView**：設定画面（SwiftUI）

UI 層（SettingsView、区切りアイテムの見た目）とロジック層（SectionManager、SettingsStore）を分離し、ロジック層は AppKit への依存をプロトコルで抽象化してテスト可能にする。

## テスト方針

t_wada 流の TDD で進める。実装より先にテストを書き、レッド、グリーン、リファクタリングの順で回す。

- **ユニットテスト**：SectionManager の状態遷移（トグル、サブバー自動クローズのタイマー制御）、SettingsStore の読み書きを対象とする。NSStatusItem はプロトコルで差し替え、モックで検証する
- **手動確認項目**：実機での区切りアイテムの押し出し動作、Cmd ドラッグでの配置、ログイン時起動。これらは OS 統合部分でありユニットテストの対象外とする

## 後続フェーズ

MVP の完成後、次の順で拡張を検討する。

1. メニューバーの外観カスタマイズ
2. アイコンの検索とレイアウトプロファイル
3. コード署名、公証、Homebrew 配布
